{
  "version": "0.2.0",
  "configurations": [{
    "name": "Launch dev server",
    "type": "node2",
    "request": "launch",
    "program": "${workspaceRoot}/dev-server/index.js",
    "cwd": "${workspaceRoot}",
    "preLaunchTask": "dev",
    "env": {
      "NODE_ENV": "development"
    }
  }]
}